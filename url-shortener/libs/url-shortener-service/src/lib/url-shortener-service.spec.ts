import { urlShortenerService } from './url-shortener-service';

describe('urlShortenerService', () => {
  it('should work', () => {
    expect(urlShortenerService()).toEqual('url-shortener-service');
  });
});
